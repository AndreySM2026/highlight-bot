from __future__ import annotations

import json

from services.highlights.schemas import ActivityMap


def build_highlight_prompt(activity_map: ActivityMap) -> str:
    payload = activity_map.model_dump()
    return f"""Проанализируй карту активности русскоязычного видео и найди самые яркие моменты для коротких вертикальных клипов (Reels/Shorts).

Правила длительности:
- Каждый клип: от 15 до 60 секунд.
- Клипы не должны сильно пересекаться.

Правила смысла (ОЧЕНЬ ВАЖНО):
- Клип должен начинаться с НАЧАЛА мысли, фразы или вопроса — не с середины слова и не с середины предложения.
- Используй поле silent_ranges: ставь start_time сразу ПОСЛЕ паузы (silence_end), когда начинается новая реплика.
- Конец клипа — на паузе или логическом завершении мысли (перед silence_start), а не обрыв на полуслове.
- Каждый клип должен быть понятен зрителю БЕЗ контекста всего видео: зритель сразу понимает, о чём речь.
- Предпочитай: яркие тезисы, провокационные утверждения, завершённые мини-истории, ответы на вопросы.
- recommended_clip_count — оптимальное число клипов (от 1 до 10).

Ответь ТОЛЬКО валидным JSON без markdown и комментариев.

Формат ответа:
{{
  "recommended_clip_count": 5,
  "segments": [
    {{
      "start_time": 45.0,
      "end_time": 95.0,
      "score": 0.91,
      "title": "Краткое название момента",
      "reason": "Почему этот момент яркий и самодостаточен"
    }}
  ]
}}

Карта активности (windows — окна по 20 сек, silent_ranges — паузы в речи):
{json.dumps(payload, ensure_ascii=False, indent=2)}
"""
